function calculateCartPrice(...num1){ // here ... act as a rest operator we see that this is used as a spread oprator also
  return num1
}

console.log(calculateCartPrice(100 ,200 , 300)) // this returns an array 